@model Hinata.Models.IndexViewModel
@{
    Layout = "~/Views/Shared/_LayoutWithAccount.cshtml";
    ViewBag.Title = "管理";
}
<div class="container">
    <h2>@ViewBag.Title</h2>

    <p class="text-success">@ViewBag.StatusMessage</p>
    <div>
        <h4>アカウント設定の変更</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>パスワード:</dt>
            <dd>
                [
                    @Html.ActionLink("パスワードの変更", "ChangePassword")
                ]
            </dd>
            @*<dt>外部ログイン:</dt>
                <dd>
                    @Model.Logins.Count [
                    @Html.ActionLink("管理", "ManageLogins") ]
                </dd>*@
            @*
                電話番号は、2 要素認証システムでの確認のための、2 つ目の要素として使用できます。

                 SMS を使用した 2 要素認証がサポートされるようにこの ASP.NET アプリケーションを設定する方法の
                    詳細については、<a href="http://go.microsoft.com/fwlink/?LinkId=313242">この記事</a>をご覧ください。

                 2 要素認証のセットアップが完了したら、次のブロックのコメントを解除してください。
            *@
            @*
                <dt>電話番号:</dt>
                <dd>
                    @(Model.PhoneNumber ?? "なし") [
                    @if (Model.PhoneNumber != null)
                    {
                        @Html.ActionLink("変更", "AddPhoneNumber")
                        @: &nbsp;|&nbsp;
                        @Html.ActionLink("削除", "RemovePhoneNumber")
                    }
                    else
                    {
                        @Html.ActionLink("追加", "AddPhoneNumber")
                    }
                    ]
                </dd>
            *@
            @*<dt>2 要素認証:</dt>
                <dd>
                    <p>
                        2 要素認証プロバイダーは構成されていません。2 要素認証がサポートされるようにこの ASP.NET アプリケーションを設定する方法の詳細については、
                        <a href="http://go.microsoft.com/fwlink/?LinkId=313242">この記事</a>をご覧ください。
                    </p>
                    @if (Model.TwoFactor)
                        {

                            <form method="post" action="/Manage/DisableTwoFactorAuthentication">
                                有効
                                <input type="submit" value="無効にする" class="btn btn-link" />
                            </form>
                        }
                        else
                        {

                            <form method="post" action="/Manage/EnableTwoFactorAuthentication">
                                無効
                                <input type="submit" value="有効にする" class="btn btn-link" />
                            </form>
                        }
                </dd>*@
        </dl>
    </div>
</div>